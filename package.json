{
  "name": "kinfolk",
  "private": true,
  "version": "0.1.0",
  "description": "DL-Genealogy — Interactive Family Archive Platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:seed": "pnpm --filter api db:seed",
    "db:push": "pnpm --filter api db:push"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.15.0"
}
